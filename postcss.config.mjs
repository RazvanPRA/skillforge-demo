// Tailwind v4 este procesat prin PostCSS, astfel incat clasele utilitare sa fie disponibile in intreaga aplicatie.
const config = {
  plugins: {
    "@tailwindcss/postcss": {}
  }
};

export default config;
